import { launch_launchRocket, launch_deploySupport } from "../types";

export const launchRocket = () => (
    {
        type: launch_launchRocket,
    }
)


export const deploySupport = () => (
    {
        type: launch_deploySupport,
    }
)